pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                sh 'build SquareCalculator.javac'
                sh 'echo SquareCalculator'
            }
        }
    }
}
